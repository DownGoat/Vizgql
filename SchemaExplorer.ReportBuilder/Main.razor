@using SchemaExplorer.Core
@using SchemaExplorer.Core.Types
<div class="root-types">
    @foreach (var root in Schema.RootTypes)
    {
        <RootTypeComponent RootType="root" />
    }
</div>

@code {
    [Parameter]
    public SchemaType Schema { get; set; }
}